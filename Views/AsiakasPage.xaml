<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="tuotanto1.Views.AsiakasPage">

    <VerticalStackLayout Padding="20">

        <Button Text="Lisää asiakas"
                Clicked="LisaaAsiakas_Clicked" />

        <CollectionView x:Name="AsiakasLista">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <VerticalStackLayout Padding="10">
                        <Label Text="{Binding Etunimi}" FontSize="20" />
                        <Label Text="{Binding Sukunimi}" FontSize="20" />
                        <Label Text="{Binding Email}" FontSize="14" />
                    </VerticalStackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

    </VerticalStackLayout>
</ContentPage>
